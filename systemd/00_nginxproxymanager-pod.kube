[Install]
WantedBy=default.target

[Unit]
Wants=podman-user-wait-network-online.service
After=podman-user-wait-network-online.service

[Service]
#ExecStartPre=/bin/sleep 5

[Kube]
# Point to the yaml file in the same directory
Yaml=/srv/apps/00_nginxproxymanager/00_nginxproxymanager-pod.yaml
Network=homeserver01-network
# Use  config map in the same directory
# ConfigMap=/srv/apps/00_nginxproxymanager/00_nginxproxymanager_configmap.yaml
